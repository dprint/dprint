[package]
name = "test-plugin"
version = "0.1.0"
authors = ["David Sherret <dsherret@gmail.com>"]
edition = "2018"

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
dprint-core = { path = "../../../core" }
serde = { version = "1.0.88", features = ["derive"] }
serde_json = "1.0"
