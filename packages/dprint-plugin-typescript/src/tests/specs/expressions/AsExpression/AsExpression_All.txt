~~ lineWidth: 40 ~~
== should format ==
return 5   as    string;

[expect]
return 5 as string;

== should format when multiple lines ==
return 5 as string | number | other | other;

[expect]
return 5 as string | number | other
    | other;

== should indent the as expression when initial expression is hanging and the as expression is hanging ==
return testing()
    .find(p => p.testing) as string | undefined;

[expect]
return testing()
    .find(p => p.testing) as string
        | undefined;
