~~ lineWidth: 40 ~~
== should parse the as expression ==
return 5   as    string;

[expect]
return 5 as string;

== should parse when multiple lines ==
return 5 as string | number | other | other;

[expect]
return 5 as string | number | other
    | other;
