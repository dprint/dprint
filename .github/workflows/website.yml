# GENERATED BY ./website.generate.ts -- DO NOT DIRECTLY EDIT

name: Website
on:
  workflow_dispatch:
    inputs:
      deploy:
        description: Deploy to GitHub Pages
        type: boolean
        default: false
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false
      - uses: denoland/setup-deno@667a34cdef165d8d2b2e98dde39547c9daac7282
        with:
          cache: true
          deno-version: canary
      - name: Install dependencies
        run: (cd website && deno install)
      - name: Build playground
        run: (cd website/playground && deno task build)
      - name: Build website
        run: "(cd website && deno task build:ci)"
      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@132898c54c57c7cc6b80eb3a89968de8fc283505
        if: github.event_name == 'workflow_dispatch' && inputs.deploy
        with:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          BRANCH: gh-pages
          FOLDER: website/_site
          single-commit: true

# gagen:pin actions/checkout@v6 = de0fac2e4500dabe0009e67214ff5f5447ce83dd
# gagen:pin denoland/setup-deno@v2 = 667a34cdef165d8d2b2e98dde39547c9daac7282
# gagen:pin JamesIves/github-pages-deploy-action@releases/v3 = 132898c54c57c7cc6b80eb3a89968de8fc283505
