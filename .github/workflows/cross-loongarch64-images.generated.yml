# GENERATED BY ./cross-loongarch64-images.ts -- DO NOT DIRECTLY EDIT

name: Cross Images
on:
  workflow_dispatch: {}
  push:
    branches:
      - main
    paths:
      - .github/workflows/cross-loongarch64.Dockerfile
      - .github/workflows/cross-loongarch64-images.generated.yml
jobs:
  build:
    name: '${{ matrix.target }}'
    if: github.repository == 'dprint/dprint'
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      packages: write
    defaults:
      run:
        shell: bash
    strategy:
      matrix:
        target:
          - loongarch64-unknown-linux-gnu
          - loongarch64-unknown-linux-musl
      fail-fast: false
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
      - uses: docker/setup-qemu-action@06116385d9baf250c9f4dcb4858b16962ea869c3 # v4
      - uses: docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5 # v4
      - name: Login to ghcr.io
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ghcr.io
          username: '${{ github.actor }}'
          password: '${{ secrets.GITHUB_TOKEN }}'
      - name: Build and push
        uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf # v7
        with:
          file: .github/workflows/cross-loongarch64.Dockerfile
          push: true
          tags: |-
            ghcr.io/dprint/dprint-cross-base-${{ matrix.target }}:7efd4907cac7
            ghcr.io/dprint/dprint-cross-base-${{ matrix.target }}:latest
          cache-from: 'type=gha,scope=dprint-cross-base-${{ matrix.target }}-7efd4907cac7'
          cache-to: 'type=gha,mode=max,scope=dprint-cross-base-${{ matrix.target }}-7efd4907cac7'
          build-args: |-
            CROSS_BASE_IMAGE=ghcr.io/cross-rs/${{ matrix.target }}:main
            TARGET=${{ matrix.target }}
